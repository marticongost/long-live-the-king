import * as palette from './palette';

export const controlBackgroundColor = palette.carrara;
export const controlBorderColor = palette.gurkha;
export const controlBorder = `1px solid ${controlBorderColor}`;
export const controlHeight = '2em';
